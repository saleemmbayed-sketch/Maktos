# Quote Follow-Up Bundle

This folder is reserved for the structured quote follow-up strategist bundle.

Raw source outputs are stored in:

- `strategy/raw_outputs/quote_followup/`

Current status:

- Raw Strategy Studio outputs imported.
- Structured bundle files not yet created.
- Not executable by CampaignOps.

Expected bundle contract:

- `campaign_spec.yaml`
- `icp_segments.yaml`
- `message_matrix.yaml`
- `measurement_plan.yaml`
- `compliance_review.yaml`
- `STRATEGIST_BUNDLE.md`
